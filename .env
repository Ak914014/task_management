# DB_CONNECTION="postgresql://username:password@localhost:5432/task_management_db"  this is the connection string for the database. It specifies that we are using PostgreSQL as the database, with the username "username", password "password", and connecting to a database named "task_management_db" on the local machine (localhost) at port 5432.
DB_CONNECTION="postgresql://ajay:ajay123@localhost:5432/task_management_db"